import React from "react";
import styled from "styled-components";

const Container = styled.section``;

const SearchMain = () => {
  const categoryList = ["소설/시/희곡", "경제경영", "자기계발", "에세이", ""];

  return <Container>SearchMain</Container>;
};

export default SearchMain;
